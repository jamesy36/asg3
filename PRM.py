import socket
import time


class PRM():
	def _init_(self,id):


	def rcvPrep(self,data, channel):
		#prepare for transfer

	def rcvAck(self, data, channel):
		#it needs to be ack 

	def rcvCLI(self):
		#it needs to be commanded by the CLI to determine what function 
		#to do

	def rcvMessage(self, data?):
		#it needs to receive the message